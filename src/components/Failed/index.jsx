import React from 'react';
import { Header, ErrorMsg } from './style';

const Failed = () => (
  <>
    <Header> People of Star Wars </Header>
    <ErrorMsg>Failed to Load  Data</ErrorMsg>
  </>
);
export default Failed;
